# Security and Secrets

Do not include secrets in issues, logs, screenshots, PRs, or commits.

Never publish:

- Database URLs
- API keys
- Runner tokens
- OAuth secrets
- Cloud credentials

Report vulnerabilities using `SECURITY.md`.
